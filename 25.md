# 异闻录

<!-- 1. 搜索框与结果容器 -->
<!-- 样式修改：border 改为 solid，background 改为 #6a89e6 -->
<div id="page-search-wrapper" style="margin: 20px 0; padding: 15px; border: 2px solid #4e67b0; border-radius: 8px; background: #b6c5f0; color: white;">
    <input type="text" id="page-search-input" placeholder="搜索本页..." 
        style="width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 5px; margin-bottom: 10px; color: #333;">
    
<div id="search-results-area" style="display: none;">
        <p style="color: white; font-weight: bold; margin-bottom: 5px;">✅ 文本匹配结果：</p>
        <div id="results-container" style="background: white; padding: 10px; border-left: 4px solid #28a745; max-height: 400px; overflow-y: auto; box-shadow: inset 0 0 5px rgba(0,0,0,0.1); color: #333;">
        </div>
        <hr style="border: 0; border-top: 1px solid rgba(255,255,255,0.3);">
    </div>
</div>

<!-- 2. 核心逻辑（保持不变，继续过滤代码块） -->
<script>
document.getElementById('page-search-input').addEventListener('input', function() {
    const keyword = this.value.trim().toLowerCase();
    const resultsArea = document.getElementById('search-results-area');
    const container = document.getElementById('results-container');
    
    container.innerHTML = '';
    
    if (keyword.length < 1) {
        resultsArea.style.display = 'none';
        return;
    }

    const content = document.querySelector('article') || document.querySelector('.post-content') || document.body;
    const elements = content.querySelectorAll('p, li, h1, h2, h3, h4, h5, h6');
    let hasMatch = false;

    elements.forEach(el => {
        // 过滤搜索框区域及代码块
        if (document.getElementById('page-search-wrapper').contains(el)) return;
        if (el.tagName === 'CODE' || el.tagName === 'PRE' || el.closest('pre') || el.closest('code')) {
            return; 
        }

        const text = el.innerText;
        if (text.toLowerCase().includes(keyword)) {
            hasMatch = true;
            const clone = el.cloneNode(true);
            
            // 清洗掉克隆内容中的行内代码
            const codeInClone = clone.querySelectorAll('code');
            codeInClone.forEach(c => c.remove()); 

            if (clone.id) clone.removeAttribute('id');
            
            const regex = new RegExp(`(${keyword})`, 'gi');
            const cleanText = clone.innerText; 
            clone.innerHTML = cleanText.replace(regex, '<mark style="background: #ffeb3b; color: #000;">$1</mark>');
            
            clone.style.cursor = 'pointer';
            clone.style.marginBottom = '10px';
            clone.onclick = () => el.scrollIntoView({ behavior: 'smooth', block: 'center' });
            
            container.appendChild(clone);
        }
    });

    resultsArea.style.display = 'block';
    if (!hasMatch) {
        container.innerHTML = '<p style="color: #999;">未找到匹配的文本内容</p>';
    }
});
</script>

本栏目主要用于记录各类神奇事物记录，各位尽请欣赏。
<p>
<a href="100/101.html" style="color: #0366d6;">1.神奇淫叫</a>
</p>

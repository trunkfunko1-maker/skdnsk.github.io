# 异闻录

<!-- 1. 搜索框与结果容器 (保持不变) -->
<div id="page-search-wrapper" style="margin: 20px 0; padding: 15px; border: 2px dashed #6784db; border-radius: 8px; background: #f9fff9;">
    <input type="text" id="page-search-input" placeholder="搜索本页..." 
        style="width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 5px; margin-bottom: 10px;">
    
<div id="search-results-area" style="display: none;">
        <p style="color: #1d20e0; font-weight: bold; margin-bottom: 5px;">✅ 文本匹配结果：</p>
        <div id="results-container" style="background: white; padding: 10px; border-left: 4px solid #28a745; max-height: 400px; overflow-y: auto; box-shadow: inset 0 0 5px rgba(0,0,0,0.1);">
        </div>
        <hr>
    </div>
</div>

<!-- 2. 改进后的过滤逻辑 -->
<script>
document.getElementById('page-search-input').addEventListener('input', function() {
    const keyword = this.value.trim().toLowerCase();
    const resultsArea = document.getElementById('search-results-area');
    const container = document.getElementById('results-container');
    
    container.innerHTML = '';
    
    if (keyword.length < 1) {
        resultsArea.style.display = 'none';
        return;
    }

    const content = document.querySelector('article') || document.querySelector('.post-content') || document.body;
    
    // 依然选取常见的文本标签
    const elements = content.querySelectorAll('p, li, h1, h2, h3, h4, h5, h6');
    let hasMatch = false;

    elements.forEach(el => {
        // --- 核心过滤逻辑 ---
        // 1. 跳过搜索框区域本身
        if (document.getElementById('page-search-wrapper').contains(el)) return;
        
        // 2. 检查当前元素是否是代码标签，或者是否被包裹在代码标签内
        // closest('pre') 会向上查找，如果父级有 <pre> 则证明是代码块
        if (el.tagName === 'CODE' || el.tagName === 'PRE' || el.closest('pre') || el.closest('code')) {
            return; 
        }

        // 只提取纯文本进行匹配，innerText 会过滤掉 HTML 标签
        const text = el.innerText;
        
        if (text.toLowerCase().includes(keyword)) {
            hasMatch = true;
            
            // 克隆元素
            const clone = el.cloneNode(true);
            
            // 如果段落内部含有行内代码 <code>，克隆后也将其移除或清空内容（确保置顶区也没代码）
            const codeInClone = clone.querySelectorAll('code');
            codeInClone.forEach(c => c.remove()); 

            if (clone.id) clone.removeAttribute('id');
            
            // 高亮文本
            const regex = new RegExp(`(${keyword})`, 'gi');
            // 注意：这里用 innerText 赋值，确保不会因为关键词匹配到 HTML 属性而崩坏
            const cleanText = clone.innerText; 
            clone.innerHTML = cleanText.replace(regex, '<mark style="background: #c3e6cb; color: #155724;">$1</mark>');
            
            clone.style.cursor = 'pointer';
            clone.style.marginBottom = '10px';
            clone.onclick = () => el.scrollIntoView({ behavior: 'smooth', block: 'center' });
            
            container.appendChild(clone);
        }
    });

    resultsArea.style.display = 'block';
    if (!hasMatch) {
        container.innerHTML = '<p style="color: #999;">未找到匹配的文本内容</p>';
    }
});
</script>

本栏目主要用于记录各类神奇事物记录，各位尽请欣赏。
<p>
<a href="101.html" style="color: #0366d6;">1.神奇淫叫</a>
</p>
